#!/usr/bin/env bash
# Display all active IPv4 IPs of the machine

IPBIN="/sbin/ip"
if ! command -v "$IPBIN" >/dev/null 2>&1; then
  IPBIN="/usr/sbin/ip"
fi

if ! command -v "$IPBIN" >/dev/null 2>&1; then
  IPBIN="ip"
fi

"$IPBIN" -4 -o addr show 2>/dev/null | awk '{print $4}' | cut -d/ -f1